server {
  listen 80;
  error_page 503 /index.html;
  location / {
    return 503;
  }
  location /index.html {
    root /usr/share/nginx/html;
    internal;
  }
}

server {
  listen 8080;
  location / {
    return 200;
  }
}
